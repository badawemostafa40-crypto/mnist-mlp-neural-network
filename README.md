# mnist-mlp-neural-network
This project implements a Multilayer Perceptron (MLP) using PyTorch to classify handwritten digits from the MNIST dataset. The project includes data preprocessing, model training, evaluation, visualization of loss and accuracy curves, confusion matrix analysis, and comparison between different experiments using ReLU and Tanh activation functions.
